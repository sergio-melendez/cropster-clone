# Python
__pycache__/
*.pyc
.venv/
venv/

# Saved roasts DB (runtime data, created by the adapter)
*.db

# Per-machine bundled-app config (written next to the exe/run_app at runtime).
# The committed roastmonitor.env.example documents the format.
roastmonitor.env

# Local Claude config (machine-specific paths, personal permissions)
.claude/

# Node
node_modules/
dist/
*.tsbuildinfo
vite.config.ts.timestamp-*.mjs

# PyInstaller build output
build/

# OS / editor
.DS_Store
.idea/
.vscode/
