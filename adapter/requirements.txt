fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12        # required for file uploads (profile import)
pypdf==5.1.0                     # parse Cropster PDF roast reports (profile import)
# Real hardware (install when your board arrives):
# Phidget22
